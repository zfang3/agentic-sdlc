# Architecture Overview

> **Status**: draft — fill in during `/start-bootstrap` and keep current via `/start-sync`.

## What this system does

*Write one paragraph describing what the system is, who uses it, and what value it provides. Be concrete. A reader unfamiliar with the project should be able to explain the system back in their own words after reading this.*

## Core concepts

*List the three to five concepts a new engineer must understand before touching the code. For each concept, give a one-sentence definition. Prefer concrete nouns from the codebase over abstract terms.*

- **\<concept\>** — \<one-sentence definition\>
- **\<concept\>** — \<one-sentence definition\>
- **\<concept\>** — \<one-sentence definition\>

## Key files

*List the three to five files a new engineer should read first. For each, say in one line why it matters.*

- `<path>` — \<why this is important\>
- `<path>` — \<why this is important\>
- `<path>` — \<why this is important\>

## Tech stack

*Language, framework, storage, deployment target, key libraries. One line per item.*

- **Language**:
- **Framework**:
- **Storage**:
- **Deploy**:
- **Key libraries**:

## High-level architecture

*Describe how the main components fit together. ASCII diagrams are welcome. Keep it terse — request-level detail belongs in a separate doc added later if needed.*

```
<diagram or paragraph>
```

## What's intentionally out of scope

*List things people might assume the system does but doesn't. Prevents scope creep and wrong-direction implementation.*

- \<out-of-scope item\>
- \<out-of-scope item\>

## Conventions worth knowing

*Project-specific conventions that aren't obvious from reading the code. Each should have a concrete example or file reference. Skip this section entirely if there's nothing unusual.*

- \<convention\> — \<short explanation, optionally with a file reference\>

## Related documents

- [Design decisions](decisions/) — ADRs for significant choices
- [Product spec](../product/spec.md) — what we're building and why
- [Roadmap](../product/roadmap.md) — what's next

## History of change

*Append a dated line when the architecture changes materially. Keep the list short — git history is the long-term archive.*

- \<YYYY-MM-DD\> — initial architecture defined
