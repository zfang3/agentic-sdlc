# /start-build conventions

> Project-specific guidance for `/start-build`. Read by the skill at the start of every run. Leave blank to use the skill's built-in defaults; anything added here layers on top of them.

## Notes for this project

*Common additions for `/start-build`:*
*- Commit message format beyond Conventional Commits (team-specific prefixes, required trailers, ticket-ID format)*
*- TDD strictness (e.g. "integration tests optional for internal-only endpoints", "UI changes require screenshot evidence")*
*- Files or directories off-limits during implementation (generated code, vendored libs, third-party patches)*
*- Preferred fixtures or factories to reuse instead of building fresh state*
*- Patterns the team prefers over common alternatives (e.g. result types over exceptions; explicit imports over wildcards)*

*(Delete the italicized hints above once you have real content. Leave the section blank to stick with plugin defaults.)*
