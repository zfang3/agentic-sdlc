# /start-address-review conventions

> Project-specific guidance for `/start-address-review`. Read by the skill at the start of every run. Leave blank to use the skill's built-in defaults; anything added here layers on top of them.

## Notes for this project

*Common additions for `/start-address-review`:*
*- Review-response tone and style (formal vs casual; how much to justify fixes)*
*- Rebuttal style — how this team phrases pushback (cite vs summarize vs quote)*
*- Bot allow-list — which bot comments to engage with (Dependabot security findings, Renovate warnings) vs ignore (lint noise, auto-approve markers)*
*- Labels to apply when address-review pushes fixes (`needs-rereview`, `addressed-feedback`, etc.)*
*- CCs or notifications expected when specific threads are addressed (e.g. loop in the security team on SECURITY-tagged comments)*

*(Delete the italicized hints above once you have real content. Leave the section blank to stick with plugin defaults.)*
