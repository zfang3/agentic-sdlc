# /compound-promote conventions

> Project-specific guidance for `/compound-promote`. Read by the skill at the start of every run. Leave blank to use the skill's built-in defaults; anything added here layers on top of them.

## Notes for this project

*Common additions for `/compound-promote`:*
*- Branch naming convention this project uses for skill-promotion PRs (e.g. `feat/skills/<slug>` or `<jira-id>/promote-<slug>` instead of the plugin default `skill/compound-promote-<slug>`).*
*- Commit-message format the project follows (e.g. Conventional Commits — `chore(skills): promote <slug>` — or a custom prefix).*
*- PR body template the project requires — typically reference `.github/pull_request_template.md` and tell the skill to fill that template's sections instead of the plugin default.*
*- Required reviewers / code-owner tags / labels to set on the opened PR.*
*- Whether to push directly to a remote branch or open the PR through a different mechanism (e.g. GitHub-via-Slack workflow, internal review tooling).*

*(Delete the italicized hints above once you have real content. Leave the section blank to stick with plugin defaults.)*
