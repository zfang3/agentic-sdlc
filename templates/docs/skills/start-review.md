# /start-review conventions

> Project-specific guidance for `/start-review`. Read by the skill at the start of every run. Leave blank to use the skill's built-in defaults; anything added here layers on top of them.

## Notes for this project

*Common additions for `/start-review`:*
*- Review axes specific to the project (accessibility, i18n, observability, performance targets, cost-of-ownership)*
*- Severity conventions the team uses (how CRITICAL vs IMPORTANT vs SUGGESTION is drawn here)*
*- Dependency policy — approved sources, forbidden packages, license constraints*
*- Required reviewers, CODEOWNERS pointer, or team-specific review rotations*
*- Patterns the team wants enforced on every PR (e.g. "every new endpoint has a rate-limit note", "every new env var is documented")*

*(Delete the italicized hints above once you have real content. Leave the section blank to stick with plugin defaults.)*
