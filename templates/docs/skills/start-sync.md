# /start-sync conventions

> Project-specific guidance for `/start-sync`. Read by the skill at the start of every run. Leave blank to use the skill's built-in defaults; anything added here layers on top of them.

## Notes for this project

*Common additions for `/start-sync`:*
*- Doc freshness SLAs (e.g. "ADRs older than 90 days without re-review get flagged")*
*- Highest-priority docs for this project beyond `overview.md` and `verification.md`*
*- Cadence for running `/start-sync` (weekly, per-sprint, before each release)*
*- Patterns the team routinely has to re-correct — candidates for `/compound-learn` or a new skill*
*- Files intentionally excluded from drift detection (generated docs, third-party READMEs)*

*(Delete the italicized hints above once you have real content. Leave the section blank to stick with plugin defaults.)*
